// frontend/utils/externalLoggerService.js
export const logInfoExternal = (message, data) => {
  // Simulate sending info log to an external service
};

export const logWarnExternal = (message, data) => {
  // Simulate sending warning log to an external service
};

export const logErrorExternal = (message, error) => {
  // Simulate sending error log to an external service
};
