// Importing test suites from the backend/utils directory
import './debugLogger.test';
import './gptUtils.test';
import './inputValidator.test';
import './securityScanner.test';

// Note: In Jest, importing the test files is sufficient to execute the tests contained within them.
// The tests will automatically be run when the `jest` command is executed on this file.
